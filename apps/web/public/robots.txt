# Hilads — robots.txt
#
# Allow all crawlers (Hilads has no private content on the public webapp; the
# entire surface is shareable). The PWA service worker, manifest and well-known
# files don't need explicit listing — they're allowed by default.
#
# Block crawling of in-flight upload URLs and any auth callback paths if those
# ever get added; for now the disallow list is empty.

User-agent: *
Allow: /

Sitemap: https://hilads.live/sitemap.xml
